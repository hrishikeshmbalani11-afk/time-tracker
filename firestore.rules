rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows anyone who is logged in via Google to read/write.
      // (Not as strict as a specific UID, but much safer than test mode).
      allow read, write: if request.auth != null;
    }
  }
}